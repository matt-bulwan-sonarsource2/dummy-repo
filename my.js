// This is a single-line comment in JavaScript.
/*
This is a multi-line comment.
It explains the purpose of the script.
*/

// Declare a variable using 'const' (for a constant value).
const greetingMessage = "Hello from JavaScript!";

// Define a function that takes a name as an argument and logs a personalized greeting.
function greetUser(name) {
  console.log(`${greetingMessage} My name is ${name}.`);
}

// Call the function with a specific name.
greetUser("Alice");

// Demonstrate a simple arithmetic operation.
let num1 = 10;
let num2 = 5;
let sum = num1 + num2;
console.log(`The sum of ${num1} and ${num2} is: ${sum}`);
